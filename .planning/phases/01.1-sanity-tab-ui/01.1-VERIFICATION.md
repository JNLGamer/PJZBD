---
phase: 01.1-sanity-tab-ui
verified: 2026-04-28T00:00:00Z
status: gaps_found
score: 14/14 static + 8/8 critical-runtime items pass; 2 design gaps reported by user during in-game UAT
re_verification: 2026-04-28T05:57:00Z
gaps:
  - id: GAP-01
    severity: design
    title: "Bar orientation should be vertical on the right side"
    detail: "Current implementation has a horizontal bar across the top per UI-SPEC Layout Contract and D-21. User reviewed in-game and prefers a vertical bar pinned to the right edge of the panel. This shifts the layout: log occupies the left/center, debuff row repositions accordingly."
    affects: ["D-05 (bar shape)", "D-21 (top-to-bottom layout)"]
    user_feedback: "I kinda dislike the horisonal bar, i think it should be vertical on the right side"
    suggested_fix: "Rotate bar drawing to vertical (drawRect with width=barW, fill height proportional to sanity). Pin to panel right edge. Move stage label and numeric readout to a position that makes sense with vertical bar (above bar, or to the left of it). Reflow event log + debuff row to fill freed horizontal space."

  - id: GAP-02
    severity: design
    title: "Bar should always render the full red→yellow→green gradient (like reference image), not change color with sanity level"
    detail: "Current implementation (UI-SPEC §Color, gradient interpolation rule for D-05) computes a single fill color from `pct` and fills the bar that single color — at high sanity the whole bar is green; at low sanity the whole bar is red. User wants the bar to ALWAYS display the full gradient (green at one end, red at the other), with sanity controlling how much of the bar is filled, not the color of the fill."
    affects: ["D-05 (3-stop gradient interpretation)"]
    user_feedback: "the bar is missing the gradient from green to red. the green to red should be like in the image, it shouldn't slowly turn redder when lower"
    reference_image: "User-provided gradient: smooth horizontal green→yellow→orange→red"
    suggested_fix: "Render the bar as: (1) draw a full-bar gradient backdrop (multiple drawRect bands or per-pixel lerp) from green at the 'full' end to red at the 'empty' end. (2) Mask/clip the visible portion to the current sanity fill level. The fill level reveals more of the red end as sanity drops. Threshold ticks render on top of the gradient as before. Boundary detail TBD in /gsd:plan-phase --gaps discussion."

confirmed_passing_during_uat:
  - "Item 1: Psyche tab visible at end of strip (after Temperature) — image confirmed"
  - "Item 2: Click activates panel without Lua error — console.txt clean of SanityTraits errors"
  - "Item 4: Numeric readout exact format '950 / 1000 (95%)' — image confirmed"
  - "Item 9: Kill flow end-to-end — 5 zombie kills logged, sanity 1000→950, both Phase 1 ('Zombie killed. Sanity: X -> Y') AND Plan 02 ('log[kill] Zombie killed (delta=-10)') lines firing in console"
  - "Item 15: Per-frame refresh — bar/numeric/stage update without closing tab — kill events updated values mid-session"
  - "Item 17: Phase 1 no-regression — Init loaded, Panel loader, OnCreatePlayer with profession=veteran startingSanity=1000, Zombie killed lines all present; zero Lua errors in SanityTraits namespace"
  - "Item 7 (visual subset): Stage label rendering — 'Stage: Stable' visible in panel"
  - "Item 5 (visual subset): Event log scrollable listbox renders newest-first with 'D8 09:47 - Zombie killed (-10)' format including day stamp"

still_pending_human_uat:
  - "Items 6, 8: F11 console verification of STAGE_THRESHOLDS / computeStage boundary semantics (defer to convenience)"
  - "Item 10: FIFO cap at 50 (not exercised — only 5 entries in screenshot)"
  - "Item 11: invalid log category WARN behavior (F11 console only)"
  - "Item 13, 14: debuff row populated entry + 3-line tooltip (deferred — Phase 2 will populate appliedTraits)"
  - "Item 16: persistence (save/quit/reload) — not exercised in this session"
  - "Item 18: Phase 1 save migration (would require a save created before 01.1)"
human_verification:
  - test: "Tab visible at end of strip"
    expected: "Open character info window in-game; 6 tabs visible; 6th tab labeled exactly 'Psyche' at end of strip"
    why_human: "ISCharacterInfoWindow cannot be instantiated outside a live PZ session; visual confirmation only"
    maps_to: "D-01, D-02, D-03"

  - test: "Click 'Psyche' tab activates panel without Lua error"
    expected: "Click tab; panel content visible; console.txt clean of Lua errors"
    why_human: "Engine UI lifecycle and joypad/mouse input only fire under real input + game state"
    maps_to: "D-04"

  - test: "Bar 3-stop gradient changes color with sanity"
    expected: "F11: getPlayer():getModData().SanityTraits.sanity = 100/500/900 — bar visibly transitions red -> yellow -> green"
    why_human: "Visual rendering cannot be captured programmatically"
    maps_to: "D-05"

  - test: "Numeric readout exact format"
    expected: "At any sanity, readout reads e.g. '900 / 1000 (90%)' — exact format '%d / %d (%d%%)'"
    why_human: "Visual confirmation of in-game text rendering"
    maps_to: "D-06"

  - test: "4 always-visible threshold ticks at 750/500/250/50"
    expected: "Visual count: 4 ticks at fixed bar positions regardless of current sanity value"
    why_human: "Visual confirmation only"
    maps_to: "D-07"

  - test: "F11 console — STAGE_THRESHOLDS values reachable"
    expected: "print(SanityTraits.STAGE_THRESHOLDS.sad) -> 750; .depressed -> 500; .traumatized -> 250; .desensitized -> 50"
    why_human: "Lua console only fires under live game session (TraitFactory + module loading)"
    maps_to: "D-08"

  - test: "F11 console — STAGE_NAMES has 5 thematic names"
    expected: "for k,v in pairs(SanityTraits.STAGE_NAMES) do print(k,v) end -> 5 lines: stable Stable / shaken Shaken / hollow Hollow / numb Numb / broken Broken"
    why_human: "Live module load required"
    maps_to: "D-09"

  - test: "F11 console — computeStage boundary semantics"
    expected: "computeStage(751)='stable'; (750)='shaken'; (501)='shaken'; (500)='hollow'; (251)='hollow'; (250)='numb'; (51)='numb'; (50)='broken'; (0)='broken'"
    why_human: "Live module load required"
    maps_to: "D-10"

  - test: "Kill 1 zombie — log entry appears at top of listbox"
    expected: "Listbox top entry reads 'D{day} {HH:MM} - Zombie killed (-10)'; bar visibly drops; console.txt contains BOTH the Phase 1 'Zombie killed. Sanity: X -> Y' line AND the Plan 02 'log[kill] Zombie killed (delta=-10)' line"
    why_human: "OnZombieDead only fires under live game; ModData persistence + render require live session"
    maps_to: "D-11, D-15"

  - test: "FIFO cap at 50 entries"
    expected: "After 60 logger calls (kill or console-driven): #getPlayer():getModData().SanityTraits.log == 50; oldest entries dropped from tail"
    why_human: "Requires live ModData manager; many sequential events"
    maps_to: "D-12"

  - test: "All 4 valid log categories accepted; invalid prints WARN"
    expected: "F11: SanityTraits.log('stage','s',nil); ('trait','t',nil); ('recovery','r',5); ('kill','k',-10) — no WARN line. SanityTraits.log('badcat','x',0) — prints '[SanityTraits] WARN: invalid log category badcat' and #log unchanged."
    why_human: "Live console + ModData manager required"
    maps_to: "D-13"

  - test: "Log entry shape complete"
    expected: "F11: print(md.log[1].time, .category, .delta, .text, .icon) — all fields present (icon=nil for v1)"
    why_human: "Live log array required"
    maps_to: "D-14"

  - test: "Debuff row at baseline empty; populated entry renders"
    expected: "At appliedTraits={}, zero icons, no empty boxes. Push entry via F11: table.insert(md.appliedTraits, {traitId='base:cowardly', appliedAtStage='shaken', appliedAtTime=getTimestampMs()}) — exactly 1 slot becomes visible (icon if TraitFactory has texture, else procedural fallback rect with last char 'y')"
    why_human: "Visual + TraitFactory only available in live session"
    maps_to: "D-16, D-17, D-18"

  - test: "Hover debuff slot — 3-line tooltip"
    expected: "Hover populated slot; tooltip shows 3 lines: trait label, trait description, blank line, 'Applied by Sanity Traits at Shaken'"
    why_human: "Mouse hover events only fire under real input"
    maps_to: "D-19"

  - test: "Per-frame refresh: bar/numeric/stage update on console edit"
    expected: "Modify md.sanity via F11 without closing/reopening tab — bar, readout, and stage label update on next frame"
    why_human: "Frame loop only runs in live session"
    maps_to: "D-20"

  - test: "Persistence — save/reload preserves state"
    expected: "Save, quit to main menu, reload save: tab still appears; sanity value preserved; log entries preserved; appliedTraits preserved. Critical regression check."
    why_human: "ModDataManager save/reload cycle only runs end-to-end under live PZ"
    maps_to: "Persistence row in VALIDATION.md"

  - test: "Phase 1 no-regression"
    expected: "console.txt during a 5-minute play session contains all of: '[SanityTraits] Init loaded (v1.0)', '[SanityTraits] OnCreatePlayer: profession=...', '[SanityTraits] Zombie killed. Sanity: X -> Y', and (if a survivor was killed) '[SanityTraits] Survivor killed. Sanity: X -> Y'. No Lua errors."
    why_human: "Console.txt is generated only at runtime"
    maps_to: "No Phase 1 regression row in VALIDATION.md"

  - test: "Loading a Phase 1 save (pre-01.1) gets idempotent upgrade"
    expected: "On first load: '[SanityTraits] OnCreatePlayer: upgraded existing save with log/appliedTraits fields' line appears once in console.txt; subsequent reloads of the same save do NOT print the upgrade line again. md.SanityTraits.log and .appliedTraits both exist as non-nil arrays."
    why_human: "Requires a save created before 01.1 was deployed; idempotency only verifiable by load + reload"
    maps_to: "Risk 5 mitigation (RESEARCH); D-11/D-17 migration path"
---

# Phase 01.1: Sanity Tab UI Verification Report

**Phase Goal:** Add a visible "Psyche" tab to ISCharacterInfoWindow that surfaces the previously-invisible sanity meter built in Phase 1. The tab contains a horizontal sanity bar (3-stop red→yellow→green gradient with always-visible threshold tick marks) plus numeric readout, a thematic stage label computed from the locked STAGE_THRESHOLDS table, an event log (50-entry FIFO persisted in ModData), and a debuff icon row (up to 6 active sanity-applied traits with hover tooltips). Reverses the v1 "no UI" out-of-scope decision.

**Verified:** 2026-04-28
**Status:** human_needed — all 14 automated/static must-haves verified green; 16 in-game UAT items require human verification (PZ Lua mods have no test runner; UI rendering, ModData persistence, and event-firing all require a live game session)
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

The phase has no REQ-IDs (inserted post-requirements-lock). Truths are derived from CONTEXT.md decisions D-01..D-23 plus the 4 RESEARCH critical corrections. Truths are split into AUTOMATED-VERIFIABLE (static greppable) and HUMAN-NEEDED (in-game UAT).

#### AUTOMATED-VERIFIABLE Truths (static checks)

| #  | Truth | Status | Evidence |
| -- | ----- | ------ | -------- |
| 1  | `1_SanityTraits_Init.lua` declares `SanityTraits.STAGE_THRESHOLDS` with locked values (sad=750, depressed=500, traumatized=250, desensitized=50) | VERIFIED | Lines 26-31 |
| 2  | `1_SanityTraits_Init.lua` declares `SanityTraits.STAGE_NAMES` with 5 thematic labels (Stable/Shaken/Hollow/Numb/Broken) | VERIFIED | Lines 35-41 |
| 3  | `1_SanityTraits_Init.lua` defines `function SanityTraits.computeStage(sanity)` with desensitized-first ordering using `<=` | VERIFIED | Lines 47-53 |
| 4  | `1_SanityTraits_Init.lua` declares `SanityTraits.LOG_MAX_ENTRIES = 50` and `SanityTraits.DEBUFF_SLOT_COUNT = 6` | VERIFIED | Lines 56-57 |
| 5  | `2_SanityTraits_ModData.lua` is upgrade-aware: explicit `if md.SanityTraits == nil then ... else ... end` (no early-return on existing) | VERIFIED | Lines 33, 53. Negated grep on `if md\.SanityTraits ~= nil then` returns NO matches |
| 6  | `2_SanityTraits_ModData.lua` new-character branch seeds `log = {}` and `appliedTraits = {}` in init table | VERIFIED | Lines 47, 48 |
| 7  | `2_SanityTraits_ModData.lua` loaded-save branch idempotently adds missing fields with per-field `== nil` guards | VERIFIED | Lines 57-64 |
| 8  | `3_SanityTraits_KillEvents.lua` calls `SanityTraits.log("kill", "Zombie killed", -SanityTraits.ZOMBIE_WEIGHT)` | VERIFIED | Line 24 |
| 9  | `3_SanityTraits_KillEvents.lua` calls `SanityTraits.log("kill", "Survivor killed", -SanityTraits.SURVIVOR_WEIGHT)` | VERIFIED | Line 50 |
| 10 | Phase 1 console output preserved verbatim (Zombie killed./Survivor killed. lines unchanged) | VERIFIED | Lines 20, 46 |
| 11 | `4_SanityTraits_Panel.lua` defines `SanityTraits.log` logger with whitelist + FIFO + insert-at-1 + 5-field entry shape | VERIFIED | Lines 23-66 |
| 12 | `4_SanityTraits_Panel.lua` defines `SanityPanel = ISPanelJoypad:derive("SanityPanel")` with full lifecycle (`:new`, `:initialise`, `:createChildren`, `:prerender`, `:render`) | VERIFIED | Lines 78, 80, 98, 102, 281, 287 |
| 13 | `4_SanityTraits_Panel.lua` wraps `ISCharacterInfoWindow:createChildren` with Pitfall 2 nil guard and calls `self.panel:addView("Psyche", self.sanityView)` | VERIFIED | Lines 347-372 |
| 14 | All 4 RESEARCH critical corrections + Pitfall 3 are encoded; the 4 anti-patterns they forbid are absent | VERIFIED | See Anti-Patterns table below |

**Static Score:** 14/14 truths verified

#### HUMAN-NEEDED Truths (in-game UAT)

| #  | Truth | Status | Why human |
| -- | ----- | ------ | --------- |
| 15 | "Psyche" tab is visible at end of character info window's tab strip | NEEDS_HUMAN | Visual rendering only |
| 16 | Clicking the tab activates the panel without throwing a Lua error | NEEDS_HUMAN | Live UI input + frame loop |
| 17 | Bar shows 3-stop gradient (red→yellow→green) that visibly changes with sanity | NEEDS_HUMAN | Visual rendering |
| 18 | Numeric readout shows exact `970 / 1000 (97%)` style format at all sanity values | NEEDS_HUMAN | Visual rendering |
| 19 | 4 always-visible threshold tick marks at sanity positions 750/500/250/50 | NEEDS_HUMAN | Visual rendering |
| 20 | Killing a zombie adds "Zombie killed" entry at top of event log; bar visibly drops | NEEDS_HUMAN | Live `OnZombieDead` event |
| 21 | FIFO cap enforces 50 entries on `md.SanityTraits.log` | NEEDS_HUMAN | Requires live ModData manager + many events |
| 22 | All 4 valid log categories accepted; invalid prints WARN | NEEDS_HUMAN | Requires live console |
| 23 | Empty event log displays "No events yet." placeholder | NEEDS_HUMAN | Visual rendering |
| 24 | Debuff slot row: empty by default; populated entry renders icon (or fallback rect) | NEEDS_HUMAN | Visual + TraitFactory live state |
| 25 | Hovering populated debuff slot shows 3-line tooltip with trait label, description, attribution | NEEDS_HUMAN | Mouse hover input only |
| 26 | Per-frame refresh: bar/numeric/stage update on console edit without closing tab | NEEDS_HUMAN | Live frame loop |
| 27 | Save/reload preserves bar, log, and appliedTraits state | NEEDS_HUMAN | Live save lifecycle |
| 28 | Loading a Phase 1 save fires the idempotent upgrade exactly once | NEEDS_HUMAN | Requires Phase 1 save artifact |
| 29 | Phase 1 console output (Init loaded, OnCreatePlayer, Zombie killed./Survivor killed.) still fires; no Lua errors | NEEDS_HUMAN | Console.txt only at runtime |
| 30 | Tab placement: end of strip (after vanilla "Clothing Insulation") | NEEDS_HUMAN | Visual confirmation |

**Human Score:** 0/16 (queued for user's next launch — the validation contract acknowledges this honestly via `nyquist_compliant: false` in `01.1-VALIDATION.md`)

### Required Artifacts

| Artifact | Expected | Exists | Substantive | Wired | Data flows | Status |
| -------- | -------- | ------ | ----------- | ----- | ---------- | ------ |
| `Mods/Sanity_traits/42/media/lua/client/1_SanityTraits_Init.lua` | STAGE_THRESHOLDS, STAGE_NAMES, computeStage, LOG_MAX_ENTRIES, DEBUFF_SLOT_COUNT plus existing Phase 1 constants | YES (60 lines) | YES — all 5 new symbols present, existing ones preserved | YES — referenced by 2_, 3_, 4_ at runtime | YES — namespace globals consumed downstream | VERIFIED |
| `Mods/Sanity_traits/42/media/lua/client/2_SanityTraits_ModData.lua` | Idempotent upgrade for log/appliedTraits in OnCreatePlayer (both new-char and loaded-save branches) | YES (72 lines) | YES — both branches present, old early-return removed | YES — `Events.OnCreatePlayer.Add(onCreatePlayer)` at line 71 | YES — md.SanityTraits is the source array consumed by 4_ panel | VERIFIED |
| `Mods/Sanity_traits/42/media/lua/client/3_SanityTraits_KillEvents.lua` | Two `SanityTraits.log("kill", ...)` calls after each decrement; Phase 1 print lines preserved | YES (54 lines) | YES — both logger calls present (lines 24, 50); Phase 1 prints preserved (lines 20, 46) | YES — calls reach 4_'s logger via SanityTraits.log namespace | YES — entries flow into md.log array | VERIFIED |
| `Mods/Sanity_traits/42/media/lua/client/4_SanityTraits_Panel.lua` | SanityTraits.log logger + SanityPanel class + ISCharacterInfoWindow:createChildren wrap | YES (372 lines) | YES — logger (1-68), SanityPanel class (70-335), wrap (337-372) | YES — wraps `ISCharacterInfoWindow.createChildren` at file-load time; `addView("Psyche", ...)` installs tab | NEEDS_HUMAN — render path reads md.sanity / md.log / md.appliedTraits each frame; data flow fully wired statically but visual rendering must be confirmed in-game | VERIFIED (static); render-time data flow needs human |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| `1_SanityTraits_Init.lua` constants | `2_, 3_, 4_` consumers | Numeric prefix load order (1_ before 2_/3_/4_) → namespace globals | WIRED | Confirmed: `4_` at line 198 reads `SanityTraits.STAGE_THRESHOLDS`; `4_` at line 312 reads `SanityTraits.computeStage`; `4_` at line 313 reads `SanityTraits.STAGE_NAMES` |
| `2_SanityTraits_ModData.lua` `onCreatePlayer` | ModData seeded with log/appliedTraits | `Events.OnCreatePlayer.Add(onCreatePlayer)` registration at line 71 | WIRED | New-char branch (lines 33-52) seeds full table; loaded-save branch (lines 53-68) idempotently adds missing fields |
| `3_SanityTraits_KillEvents.lua` kill handlers | `SanityTraits.log` in `4_` | Namespace global call (resolved at runtime after all files load) | WIRED | Two literal calls verified by grep at lines 24 and 50 |
| `4_SanityTraits_Panel.lua` `SanityTraits.log` | `md.SanityTraits.log` source array | `table.insert(md.SanityTraits.log, 1, entry)` + FIFO `table.remove(md.SanityTraits.log)` | WIRED | Lines 55, 60. Source-array-as-truth pattern (Pitfall 3 mitigation: NOT `removeFirst` on listbox) |
| `4_SanityTraits_Panel.lua` SanityPanel:render | `md.SanityTraits.sanity / .log / .appliedTraits` | `self.char:getModData().SanityTraits` per-frame re-read | WIRED | Line 294. `self.char` set via `getSpecificPlayer(playerNum)` at line 86; re-acquired at line 290 if nil |
| `4_SanityTraits_Panel.lua` `ISCharacterInfoWindow:createChildren` wrap | `self.panel:addView("Psyche", view)` | Vanilla `ISTabPanel:addView` API | WIRED | Lines 347-372. `origCreateChildren(self)` called first; `addView` appends 6th tab; defensive nil guard at line 347 |
| `4_SanityTraits_Panel.lua` debuff slot ISImage | Trait icon | `TraitFactory.getTrait(traitId):getTexture()` (Critical Correction 1) | WIRED | Line 168 in `getTraitTextureSafe`; called from line 247 in `refreshDebuffRow` |
| `4_SanityTraits_Panel.lua` debuff slot ISImage | Tooltip text | `slot:setMouseOverText(tipText)` with `\n` separators (Critical Correction 2) | WIRED | Lines 263-264. Multi-line content: trait:getLabel() + "\n" + trait:getDescription() + "\n\nApplied by Sanity Traits at " + stageName |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| SanityPanel sanity bar | `md.sanity` | Phase 1 `OnCreatePlayer` seed + Phase 1 kill handlers' `math.max` decrement | YES — Phase 1 already wires real data into `md.SanityTraits.sanity` (verified in Phase 1 verification) | FLOWING |
| SanityPanel numeric readout | `md.sanity` | Same as above | YES | FLOWING |
| SanityPanel stage label | `md.sanity` → `SanityTraits.computeStage` | Same as above | YES | FLOWING |
| SanityPanel event log listbox | `md.SanityTraits.log` | Plan 02 `SanityTraits.log` called by Phase 1 kill handlers (now extended in Plan 02) | YES — every kill writes a structured entry | FLOWING |
| SanityPanel debuff row | `md.SanityTraits.appliedTraits` | Phase 2 will populate; Plan 01 ships `{}` (intentionally empty in v1) | NO real data in 01.1 (correctly empty per phase scope) — but the array IS seeded by `OnCreatePlayer`, render code reads it correctly, and the empty-state behavior (no icons, no placeholder boxes) matches D-16 | EMPTY-BY-DESIGN (not HOLLOW — phase 01.1 explicitly defers population to Phase 2) |

The "EMPTY-BY-DESIGN" status for the debuff row is consistent with the phase scope: 01.1 ships the rendering loop and the seed (`appliedTraits = {}`); Phase 2 will populate the array when stage transitions fire. The render path reads the live array each frame and has been verified statically to display nothing when empty (`setVisible(false)` for all 6 slots) — exactly matching D-16.

### Behavioral Spot-Checks

PZ Lua mods have no runnable entry points outside the game engine. Per Step 7b:

> **Step 7b: SKIPPED (no runnable entry points)** — there is no test runner, no CLI, no module that can be loaded with `node` or `lua` standalone (the code depends on PZ engine globals: `Events`, `getPlayer`, `getSpecificPlayer`, `getTimestampMs`, `TraitFactory`, `ISPanelJoypad`, `ISScrollingListBox`, `ISImage`, `ISCharacterInfoWindow`, `getGameTime`, `UIFont`, `instanceof`). The phase's VALIDATION.md explicitly sets `nyquist_compliant: false` for this exact reason. Behavioral verification is the human UAT.

### Requirements Coverage

This phase has **no REQ-IDs** assigned. CONTEXT decisions D-01..D-23 are the contract. REQUIREMENTS.md does not list any REQ-ID as mapped to Phase 01.1 (verified via section "Traceability" — no rows reference 01.1). Therefore:
- No SATISFIED requirements (no REQ-IDs to satisfy in this phase)
- No BLOCKED requirements
- No ORPHANED requirements (REQUIREMENTS.md correctly omits 01.1 from the traceability table)

The phase is fully traced via the 23 CONTEXT decisions, all of which map to either AUTOMATED-VERIFIABLE truths (static greps) or HUMAN-NEEDED truths (in-game UAT) above.

### Anti-Patterns Found

Anti-pattern scan run on all 4 source files. Result: **NO blocker anti-patterns**.

| File | Pattern Searched | Severity (if found) | Status |
| ---- | ---------------- | -------------------- | ------ |
| 1_/2_/3_/4_*.lua | `TODO\|FIXME\|XXX\|HACK\|PLACEHOLDER` | Warning | NONE FOUND |
| 4_SanityTraits_Panel.lua | `getTexture("media/ui/Traits/Trait_` (Critical Correction 1: forbidden path-based icon lookup) | Blocker | NONE FOUND |
| 4_SanityTraits_Panel.lua | `ISToolTip:new` (Critical Correction 2: forbidden manual tooltip instantiation) | Blocker | NONE FOUND |
| 4_SanityTraits_Panel.lua | `tabTotalWidth` (Critical Correction 3: forbidden — vanilla local) | Blocker | NONE FOUND |
| 4_SanityTraits_Panel.lua | `removeFirst()` (Pitfall 3: forbidden — broken vanilla method) | Blocker | NONE FOUND |
| 4_SanityTraits_Panel.lua | `getPlayer()` outside the logger (D-22: panel must use `getSpecificPlayer`) | Warning | LOGGER ONLY (line 38). Panel render code uses `getSpecificPlayer` at lines 86, 290 (D-22 satisfied) |
| 2_SanityTraits_ModData.lua | `if md\.SanityTraits ~= nil then` (Plan 01 was supposed to remove this) | Blocker | NONE FOUND (correctly removed) |

The single `getPlayer()` call (line 38 in `SanityTraits.log` logger) is intentional and allowed: the logger is a global helper that runs from any caller (kill handlers, future Phase 2/3/5 callers), not the panel render path. D-22 specifically scopes to the panel; the logger predates the player-handle decision.

### Human Verification Required

See the `human_verification:` array in the frontmatter for the full structured list. The 18 in-game UAT items (deduplicated from VALIDATION.md's 24-row table — D-08/D-09/D-10 collapse to 3 console expressions, D-11 and D-15 collapse to one zombie-kill check, etc.) cover every observable behavior a human must confirm to consider Phase 01.1 fully shipped.

The user should:
1. Launch PZ with the mod enabled.
2. Start a NEW character; open F11 Lua console.
3. Run the F11 expressions to verify D-08/D-09/D-10/Plan 02 logger contract.
4. Open the character info window; visually confirm the "Psyche" tab + bar gradient + ticks + numeric + stage + log + (empty) debuff row.
5. Kill a zombie; confirm bar drops, log entry appears, both Phase 1 and Plan 02 console lines fire.
6. Push a test entry to `appliedTraits`; confirm one debuff slot becomes visible; hover for 3-line tooltip.
7. Save, quit, reload; confirm all state preserved.
8. (Optional regression) load a Phase 1 save; confirm idempotent upgrade prints once.

### Gaps Summary

**No gaps blocking the static verification.** All 14 static must-haves pass. All 4 RESEARCH critical corrections are present in code; all 4 forbidden anti-patterns are absent. The wrap pattern, ModData seeding, idempotent upgrade, logger API, panel class, and tab installation are all wired correctly per the plan and CONTEXT specifications.

**Gaps are exclusively in the runtime/UAT bucket:** every behavioral verification (visual rendering, click-doesn't-crash, persistence, multi-step interaction) requires a live PZ session and is enumerated in the `human_verification:` frontmatter section. This is consistent with the VALIDATION.md `nyquist_compliant: false` note: PZ Lua has no automated test runner, so manual UAT IS the verification path.

**Recommended next action:** the user runs the 18 enumerated UAT items at next launch. If any UAT row fails, this VERIFICATION.md should be re-run to convert that human_verification item into a `gaps:` entry for `/gsd:plan-phase --gaps` (e.g. if the tab doesn't appear, or save/reload loses state).

---

_Verified: 2026-04-28_
_Verifier: Claude (gsd-verifier)_
