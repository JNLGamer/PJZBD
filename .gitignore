# Game installation files — only track mod source code
ProjectZomboid/

# OS
.DS_Store
Thumbs.db

# Editor / tooling
.vscode/
.idea/

# Internal planning, design notes, and AI tooling artifacts
.planning/
.claude/
.agents/
CLAUDE.md
AGENTS.md

# Local-only reference material that may include third-party copyrighted sources
modPlanner/reference/

# Workshop mods downloaded for reference — not original work
modPlanner/stolen workshop/

# Secrets — never commit credentials, tokens, or local config
.env
.env.*
!.env.example
*.key
*.pem
*.p12
*.pfx
*.crt
*.cer
*.der
*.keystore
*.jks
id_rsa
id_rsa.*
id_ed25519
id_ed25519.*
*.token
*_token
*_token.txt
*.secret
secrets.*
credentials.*
*credentials*
*.local.json
config.local.*
.netrc
.npmrc
.pypirc
auth.json
gh-token.*

# Logs that may leak paths or tokens
*.log
logs/
